{
  "name": "@manzano/services-react",
  "version": "1.0.2",
  "main": "index.js",
  "config": {
    "distFolder": "dist"
  },
  "scripts": {
    "build": "rm -rf $npm_package_config_distFolder && tsc && cp package.json $npm_package_config_distFolder",
    "deploy": "npm publish $npm_package_config_distFolder --access public",
    "prettify": "prettier src --write"
  },
  "peerDependencies": {
    "react": ">=16.8.0",
    "@manzano/services": ">=1.0.3"
  },
  "devDependencies": {
    "@manzano/services": "1.0.3",
    "@types/react": "16.9.19",
    "prettier": "2.0.5",
    "react": "16.12.0",
    "typescript": "3.9.3"
  }
}
